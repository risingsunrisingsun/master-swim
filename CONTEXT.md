# 마스터즈 수영 훈련 도구

20~30명 규모 마스터즈 동호회의 **개인 훈련 처방 도구**.

기록 아카이빙·랭킹·대회 일정은 [마이랭킹](http://www.myranking.co.kr/)과 팀 네이버 카페가
이미 하고 있다. **그것들은 만들지 않는다.** 이 앱의 자리는 목표 페이스 기반 훈련 처방과
병목 진단이다.

## 범위

| | |
| --- | --- |
| 영법 | 자유형 · 배영 · 평영 · 접영 (+ 개인혼영 100은 **기록 보관 전용**) |
| 거리 | 25m · 50m · 100m |
| 수영장 | **25m 단수로 고정** |
| 사용자 | 동호회 회원 20~30명 + 코치 1명 |

100m 이상을 지원하지 않으므로 전 종목이 스프린트다. 임계속도(CSS) 테스트는 400/200m를
전제하므로 **이 앱에서는 성립하지 않는다.** 페이스 기준점은 목표 레이스 페이스다(ADR-0003).

## 용어

- **목표 페이스 (race pace)** — 목표기록에서 산출한 25m당 목표 시간. 이 앱의 모든 세트가
  이 한 숫자에 매달려 있다. 훈련 반복은 벽 푸시오프로 출발하므로 다이빙 이득을 보정한다.
- **다이빙 이득 (dive advantage)** — 대회 첫 구간의 다이빙 스타트가 벽 푸시오프 대비 갖는
  시간 이득. `DIVE_ADVANTAGE_CS = 70` (0.70초).
- **병목 (bottleneck)** — 목표 미달의 지배적 원인. 구간 감속률 · DPS/SR 균형 · 턴 손실 중
  하나로 특정한다. "체형 때문"이라는 결론은 내지 않는다(ADR-0001).
- **DPS** — 스트로크당 거리(distance per stroke). **SR** — 스트로크 레이트.
- **등급 (level)** — 초급 · 중급 · 고급 · 최상급. 기록 축(강도)과 훈련량 축(분량)을
  각각 통과시켜 배정한다. 자가 선택이 아니다. 세트를 만들 때 **반복 수는 분량 등급**에서,
  **인터벌과 반복 거리는 강도 등급**에서 가져온다.
- **주역 / 보조 (main / support)** — 세션의 중심 세트와 그 뒤에 붙는 세트. 주역인
  레이스 페이스는 고정하고 남은 자리를 목표 거리에 따라 채운다.
- **기록 데이터 / 신체 데이터** — 저장 위치가 다른 두 계층. ADR-0002 참조.
- **기록향상반** — 코치의 집중 지도를 받는 회원 구분. 열람 권한과는 별개다.
- **위치 (standing)** — 기록이 어디쯤인지 나타내는 값. 그 종목의 **기록 분포가 있으면
  실제 백분위**, 없으면 등급 구간 보간이다. `source` 로 구분하고 화면이 항상 밝힌다(ADR-0006).
  50m 네 영법은 분포가 있고, 25m·100m 는 아직 없다.
- **이긴 비율 (percentileBeaten) / 상위 % (topPercent)** — 방향이 뒤집히기 쉬운 한 쌍.
  90% 를 이긴 사람은 **상위 10%** 다. 화면은 `topPercent` 만 쓴다.
- **훈련일 / 휴식일** — 식단이 갈리는 단위. 주간 플랜의 세션이 배정된 날이 훈련일이다.
- **섭취횟수 (serving)** — 식품군을 하루에 몇 번 먹느냐. 식사구성안의 단위이며
  이 앱은 메뉴가 아니라 이 숫자를 처방한다(ADR-0005).
- **완주 개수 (completedReps)** — 주 세트에서 목표 페이스를 지킨 반복 수.
  실패는 그 다음 번째에서 났다. 앱이 받는 유일한 훈련 결과다(ADR-0007).
- **판정 (verdict)** — 최근 3회 완주율로 목표 페이스가 이른지/맞는지/당길 때인지
  결정한 값. 3회 미만이면 판정하지 않는다.

## 화면

해시 라우팅 세 개뿐이다. 정적 호스팅에는 서버 라우터가 없다.

| 경로 | 화면 |
| --- | --- |
| `#/` | 시작 화면 — 로고와 `ENTER` 하나. 게임 타이틀처럼. 상단 바 없음 |
| `#/home` | 메뉴 — 두 기능으로 가는 타일 |
| `#/training` | 목표 기록 훈련법 — 목표 페이스 · 등급 · **하루씩 넘겨 보는** 주간 플랜 + 그날 식단 + 완주 기록 |
| `#/records` | 개인기록 추이 — 영법·거리별 시계열 차트 · 등급 위치 · 기록 표 |

**라이트 테마 하나만 지원한다**(ADR-0008). `prefers-color-scheme` 분기가 없다.

주간 플랜은 세로로 늘어놓지 않고 **하루씩** 보여준다. 폰에서 오늘 할 것만 보면
되고, 식단이 그날의 훈련 유무에 따라 달라지므로 같은 화면에 있어야 한다.

## 열람 권한

| 데이터 | 열람 |
| --- | --- |
| 대회 기록 · 구간 스플릿 | 팀 전체 (네이버 카페와 동일 수준) |
| 훈련 로그 · 세트 실패 · 진단 결과 | 코치만 |
| 신체 데이터 · 식단 | 본인만 — 서버에 존재하지 않음 |

## 단계

1. **1차 (완료 · 배포됨)** — 훈련 엔진. 목표 페이스 · 주간 플랜 · 방법 카탈로그.
   서버·로그인·외부 데이터 없이 localStorage 만으로 도는 정적 PWA.
   `https://risingsunrisingsun.github.io/master-swim/`
2. **1.5차 (완료)** — 시작 화면 · 하루씩 보는 플랜 · 식단 · 기록 추이 · 훈련 로그 ·
   **50m 네 영법 백분위**.
   식단이 4차에서 앞당겨졌다(ADR-0005). 여전히 서버가 없다.
3. **2차** — 카카오 로그인 + Supabase + 코치 대시보드.
4. **3차** — 병목 진단 (스플릿·스트로크수 입력).
   25m·100m 의 기록 분포는 자료가 들어오는 대로 붙인다(ADR-0006).

## 미해결

- **나머지 종목의 기록 분포** — **50m 는 네 영법 다 확보했다**(ADR-0006).
  남은 것은 4영법 25m·100m 다. 종목당 남녀 × 5연대 = 10장이 필요하다.
  분포가 없는 종목은 등급 구간 위치로 대체되고 화면이 그 사실을 밝힌다.
- **50대 여덟 분포의 빠른 꼬리** — 50m 네 영법 남녀 **여덟 분포 전부**에서 상위 5%
  커트라인이 젊은 연대보다 빠르게 나온다. 원본 캡처와 대조해 **전사 오류가 아님을
  확인했다** — 평지가 마이랭킹 그래프에 그대로 그려져 있다. `F/50/free` 는 중앙값까지
  40대보다 빠르다. 원본의 집계 방식을 의심하고 있고, 분위수 표를 직접 받거나
  마이랭킹의 "나의 순위 확인"으로 맞춰보는 것 외에 확인할 길이 없다
  (`distributions.ts` 머리말).
- **등급 기준 구간** — 분포가 없는 종목에 쓰는 절대 구간(100m 자유형 남자
  1:07 / 1:20 / 1:38)은 팀 현실로 검증된 값이 아니다.
- **스트로크 수 미입력자** — 해당 구간기록의 평균 스트로크수를 기본값으로 채운다.
  '본인 과거 평균'인지 '전체 평균'인지는 3차 착수 시 확정한다.

## 안고 가는 위험

건강 문진 없이 **경고 문구만** 두기로 결정했다. 앱은 실패할 때까지 반복하는 세트와
에너지 목표치를 제시하며, 20~30명 규모면 고혈압·당뇨·회전근개 문제 보유자가 포함될
가능성이 높다. 이 위험은 회원 개개인이 진다.
