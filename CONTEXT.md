# 마스터즈 수영 훈련 도구

20~30명 규모 마스터즈 동호회의 **개인 훈련 처방 도구**.

기록 아카이빙·랭킹·대회 일정은 [마이랭킹](http://www.myranking.co.kr/)과 팀 네이버 카페가
이미 하고 있다. **그것들은 만들지 않는다.** 이 앱의 자리는 목표 페이스 기반 훈련 처방과
병목 진단이다.

## 범위

| | |
| --- | --- |
| 영법 | 자유형 · 배영 · 평영 · 접영 (+ 개인혼영 100은 **기록 보관 전용**) |
| 거리 | 50m · 100m (25m 은 **기록 보관에서만** — 훈련 목표로는 고르지 못한다) |
| 수영장 | **25m 단수로 고정** |
| 사용자 | 동호회 회원 20~30명 + 코치 1명 |

100m 이상을 지원하지 않으므로 전 종목이 스프린트다. 임계속도(CSS) 테스트는 400/200m를
전제하므로 **이 앱에서는 성립하지 않는다.** 페이스 기준점은 목표 레이스 페이스다(ADR-0003).

## 용어

- **목표 페이스 (race pace)** — 목표기록에서 산출한 25m당 목표 시간. 이 앱의 모든 세트가
  이 한 숫자에 매달려 있다. 훈련 반복은 벽 푸시오프로 출발하므로 다이빙 이득을 보정한다.
- **다이빙 이득 (dive advantage)** — 대회 첫 구간의 다이빙 스타트가 벽 푸시오프 대비 갖는
  시간 이득. `DIVE_ADVANTAGE_CS = 70` (0.70초).
- **병목 (bottleneck)** — 목표 미달의 지배적 원인. 구간 감속률 · DPS/SR 균형 · 턴 손실 중
  하나로 특정한다. "체형 때문"이라는 결론은 내지 않는다(ADR-0001).
- **DPS** — 스트로크당 거리(distance per stroke). **SR** — 스트로크 레이트.
- **등급 (level)** — 초급 · 중급 · 고급 · 최상급. 기록 축(강도)과 훈련량 축(분량)을
  각각 통과시켜 배정한다. 자가 선택이 아니다 — 마법사 3단계는 **고르는 자리가 아니라
  확인하는 자리**이고, 조정은 한 단계까지만 열려 있다. 세트를 만들 때 **반복 수는 분량 등급**에서,
  **인터벌과 반복 거리는 강도 등급**에서 가져온다.
- **주역 / 보조 (main / support)** — 세션의 중심 세트와 그 뒤에 붙는 세트. 주역인
  레이스 페이스는 고정하고 남은 자리를 목표 거리에 따라 채운다.
- **기록 데이터 / 신체 데이터** — 저장 위치가 다른 두 계층. ADR-0002 참조.
- **기록향상반** — 코치의 집중 지도를 받는 회원 구분. 열람 권한과는 별개다.
- **위치 (standing)** — 기록이 어디쯤인지 나타내는 값. 그 종목의 **기록 분포가 있으면
  실제 백분위**, 없으면 등급 구간 보간이다. `source` 로 구분하고 화면이 항상 밝힌다(ADR-0006).
  **50m 네 영법만 분포가 있다.** 25m·100m 과 개인혼영은 분포를 수집하지 않기로 했고,
  등급 구간 위치를 계속 쓴다(ADR-0006).
- **이긴 비율 (percentileBeaten) / 상위 % (topPercent)** — 방향이 뒤집히기 쉬운 한 쌍.
  90% 를 이긴 사람은 **상위 10%** 다. 화면은 `topPercent` 만 쓴다.
- **훈련일 / 휴식일** — 식단이 갈리는 단위. 주간 플랜의 세션이 배정된 날이 훈련일이다.
- **이지 스윔 채우기 (filler)** — 질 위주 세트가 회원이 적은 **세션당 거리**에 못 미치면
  모자란 만큼을 회복 스윔으로 채운다. 성격마다 질 위주 거리가 벽 구간 240m ~ 지속력
  1,700m 로 7배 벌어져 있어, 채우지 않으면 목적이 순서를 바꿀 때 **무엇이 잘리느냐**로
  주간 총량이 배 넘게 흔들리고 그날 식단 열량까지 함께 움직였다. **반복 수를 늘려
  채우지 않는다** — 반복 수는 분량 등급이 정하고 그 등급을 만드는 재료가 바로 이
  세션당 거리다. 같은 값으로 반복 수를 다시 곱하면 등급이 눌러 둔 상한을 등급의 재료가
  뚫는다(주 2회 회원에게 레이스 페이스 18회). 채우는 것은 **강도가 아니라 거리**다.
  이미 회복 스윔이 든 세션은 새로 붙이지 않고 그 세트를 늘린다. 한 세션에 2,000m 까지만
  채운다 — 주간 거리와 헷갈려 크게 적은 값에 훈련이 통째로 끌려가지 않게 한다.
- **섭취횟수 (serving)** — 식품군을 하루에 몇 번 먹느냐. 식사구성안의 단위이며
  이 앱은 메뉴가 아니라 이 숫자를 처방한다(ADR-0005).
- **어록 (quote)** — 메뉴 화면 아래에 뜨는 카드. 두 종류다. **선수의 말**은 출처가
  확인된 것과 아닌 것이 섞여 있고 **화면이 항상 구별해 보여준다**(ADR-0009). 사진은
  자유 라이선스만 쓰고 저작자·라이선스를 함께 띄운다 — 그것이 CC BY 계열의 조건이다.
  **지은이 없는 문구**는 귀속시킬 사람이 없으므로 이름·출처·사진이 타입에 아예 없다.
- **지상훈련 세트 (dryland set)** — 영법별 참고 자료(`dryland.ts`). 주간 플랜에 끼워
  넣는 **등급별 처방**(`methods.ts`)과 역할이 다르다. 겹치면 `method` 로 그쪽을
  가리키고, 세트·반복 수의 원본은 언제나 `methods.ts` 다.
- **완주 개수 (completedReps)** — 주 세트에서 목표 페이스를 지킨 반복 수.
  실패는 그 다음 번째에서 났다. 앱이 받는 유일한 훈련 결과다(ADR-0007).
- **판정 (verdict)** — 최근 3회 완주율로 목표 페이스가 이른지/맞는지/당길 때인지
  결정한 값. 3회 미만이면 판정하지 않는다.

## 화면

해시 라우팅 여섯 개뿐이다. 정적 호스팅에는 서버 라우터가 없다.

| 경로 | 화면 |
| --- | --- |
| `#/` | 시작 화면 — 로고와 `ENTER` 하나. 게임 타이틀처럼. **상단 바도 주의문도 없다** |
| `#/home` | 메뉴 — 네 기능으로 가는 타일 + 팀 카페(훈련일정) 링크 + 맨 아래 **선수 어록** |
| `#/training` | 목표 기록 훈련법 — **다섯 단계 마법사**로 받고, 목표 페이스 · 등급 · **하루씩 넘겨 보는** 주간 플랜 + 그날 식단 + 완주 기록 |
| `#/records` | 개인기록 추이 — 영법·거리별 시계열 차트 · 등급 위치 · 기록 표 |
| `#/dryland` | 지상훈련 세트 — 공통 4 + 영법별 1. **검색** + 유튜브 영상 링크·미리보기 |
| `#/terms` | 수영용어 — **검색** + 분류별 용어집 + 팝업 퀴즈 10문제 |

**라이트 테마 하나만 지원한다**(ADR-0008). `prefers-color-scheme` 분기가 없다.

훈련 화면은 열두 칸을 한 폼에 늘어놓는 대신 **한 번에 하나씩 묻는다** — 종목 · 훈련량 ·
등급 확인 · 목적 · 방식의 다섯 단계다. 저장된 프로필이 있으면 **마법사를 건너뛰고 결과부터**
보여준다. 다시 온 회원에게 다섯 단계를 또 밟게 하면 예전 폼보다 나쁘다.

**등급은 3단계에서 고르는 것이 아니라 확인하는 것이다.** 기록과 훈련량에서 계산한 값을
문장으로 펴 보여주고, 한 단계까지만 옮길 수 있는 비상구를 접어 둔다.

**목적**(기록 단축 · 영법 교정 · 호흡 · 부상 예방)은 세션의 **순서만** 바꾼다. 어느 것을
골라도 여섯 성격(레이스 페이스 · 최대 속도 · 벽 구간 · 기술 · 호흡 · 지속력)이 주 6회면
다 들어가고 고른 것이 앞으로 온다 — 한 가지만 파면 나머지가 무너진다. **최대 속도는 어느
목적에서도 빼지 않는다.** 뒤로 밀 수는 있어도 빼면 전 종목이 스프린트인 앱에서 목표
페이스의 천장이 그대로다. **방식**(수중 · 지상 · 밴드)은 세션에 붙는 지상훈련을 고르지만 붙는 자리는
바꾸지 않는다. 지상훈련이 수영 뒤에 오는 것은 취향이 아니라 순서다.

주간 플랜은 세로로 늘어놓지 않고 **하루씩** 보여준다. 폰에서 오늘 할 것만 보면
되고, 식단이 그날의 훈련 유무에 따라 달라지므로 같은 화면에 있어야 한다.
그날 화면의 주인공은 세트이므로 **식단은 접힌 채로 시작한다** — 여섯 식품군 × 세 끼는
펼치면 세트를 화면 밖으로 밀어낸다.

기록 입력은 **거리가 자리수를 정한다**. 100m 면 분 칸이 나오고 25·50m 면 초 칸만
남는다. 거리를 바꾸면 값을 **접어 옮긴다** — 1:25.00 ↔ 85.00. 분 칸을 그냥 지우면
1분이 조용히 사라진다. `#/training` 과 `#/records` 가 같은 규칙을 쓴다.

뒤로 가기는 **오른쪽 위 화살표**다. 글자를 빼서 제목이 가운데를 온전히 쓴다.

## 열람 권한

| 데이터 | 열람 |
| --- | --- |
| 대회 기록 · 구간 스플릿 | 팀 전체 (네이버 카페와 동일 수준) |
| 훈련 로그 · 세트 실패 · 진단 결과 | 코치만 |
| 신체 데이터 · 식단 | 본인만 — 서버에 존재하지 않음 |

## 단계

1. **1차 (완료 · 배포됨)** — 훈련 엔진. 목표 페이스 · 주간 플랜 · 방법 카탈로그.
   서버·로그인 없이 localStorage 만으로 도는 정적 PWA.
   `https://risingsunrisingsun.github.io/master-swim/`
2. **1.5차 (완료)** — 시작 화면 · 하루씩 보는 플랜 · 식단 · 기록 추이 · 훈련 로그 ·
   **50m 네 영법 백분위**.
   식단이 4차에서 앞당겨졌다(ADR-0005). 여전히 서버가 없다.
3. **2차** — 카카오 로그인 + Supabase + 코치 대시보드.
4. **3차** — 병목 진단 (스플릿·스트로크수 입력).

## 미해결

- **등급 기준 구간** — 분포가 없는 종목에 쓰는 절대 구간(100m 자유형 남자
  1:07 / 1:20 / 1:38)은 팀 현실로 검증된 값이 아니다. 25m·100m 과 개인혼영은
  분포를 붙이지 않기로 했으므로 이 구간이 **그 종목들의 영구 경로**다. 검증하지 않은
  숫자가 계속 쓰인다는 뜻이고, 화면이 백분위가 아니라고 밝히는 것으로 버틴다.
- **스트로크 수 미입력자** — 해당 구간기록의 평균 스트로크수를 기본값으로 채운다.
  '본인 과거 평균'인지 '전체 평균'인지는 3차 착수 시 확정한다.
- **어록 세 개가 출처 미확인** — 펠프스 · 박태환 · 밀라크. 회원이 고른 문장인데
  검색으로 1차 출처가 나오지 않아 화면에 `출처 미확인` 딱지가 붙어 있다.
  출처가 나오면 `quotes.ts` 의 `source` 만 채우면 표시가 바뀐다(ADR-0009).
  펠프스는 뜻이 거의 같으면서 **확인된** 대체 문장이 주석에 적혀 있다.
- **서머 매킨토시·에릭 무삼바니는 사진이 없다** — 위키미디어 공용에도 Openverse 에도
  자유 라이선스 사진이 **없다**(찾다 만 것이 아니라 존재하지 않는다 · ADR-0009).
  팀 마크로 대신하고 있다.
- **지상훈련 영상은 내용을 보증하지 않는다** — 23개 링크 전부 YouTube oEmbed 로
  **살아 있는지만** 확인했고(제목·채널명은 거기서 그대로 받아 적었다) 재생해 보지는
  않았다. 화면이 채널명을 함께 띄워 회원이 판단하게 한다. 링크는 언젠가 죽는다 —
  죽으면 `dryland.ts` 의 `videos` 에서 빼면 된다. 미리보기 그림은 유튜브에서
  **핫링크**한다(받아두지 않는다). 못 받으면 그림만 스스로 빠지고 제목·채널명이 남는다.

- **호흡은 저호흡을 처방하지 않는다** — 숨을 참거나 줄이는 훈련은 물속 의식 소실로
  이어질 수 있고 그것은 조용히 일어난다. 동호회에는 상시 감시할 코치가 없다.
  양측 호흡 · 타이밍 · 레이스 배분만 다루고, 용어집에는 저호흡을 표제어로 두되
  **처방하지 않는다는 사실을 함께 적었다**.

## 분포의 알려진 한계 — 고치지 않기로 했다

- **50대 여덟 분포의 빠른 꼬리** — 50m 네 영법 남녀 **여덟 분포 전부**에서 상위 5%
  커트라인이 젊은 연대보다 빠르게 나온다. 원본 캡처와 대조해 **전사 오류가 아님을
  확인했다** — 평지가 마이랭킹 그래프에 그대로 그려져 있다. `F/50/free` 는 중앙값까지
  40대보다 빠르다. 원본의 집계 방식을 의심하지만 **고치지 않고 현재 값으로 간다**.
  50대 회원의 상위 % 표시는 실제보다 유리하게 나올 수 있다(`distributions.ts` 머리말).
- **자유형·배영의 ±1초 오차** — 그 두 영법은 그래프를 눈으로 읽은 추정치다. 평영·접영은
  픽셀에서 뽑아 표본 수와 정확히 맞는데, 원본 캡처 20장이 남아 있어 같은 방법으로
  다시 뽑을 수 있지만 **하지 않기로 했다.** 파일 안에 정확도가 다른 두 계층이 남는다.

## 안고 가는 위험

건강 문진 없이 **경고 문구만** 두기로 결정했다. 앱은 실패할 때까지 반복하는 세트와
에너지 목표치를 제시하며, 20~30명 규모면 고혈압·당뇨·회전근개 문제 보유자가 포함될
가능성이 높다. 이 위험은 회원 개개인이 진다.
